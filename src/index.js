import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';


//import "./styles.css"; //create a function in styles.css and import it to here... But this applies to ALL the buttons

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
);
